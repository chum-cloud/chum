# Server
PORT=3001

# Groq (LLM)
GROQ_API_KEY=gsk_...

# Supabase
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...

# Helius (Solana RPC)
HELIUS_API_KEY=...

# CHUM wallet address
CHUM_WALLET_ADDRESS=chumAA7QjpFzpEtZ2XezM8onHrt8of4w35p3VMS4C6T

# Twitter API v2 (OAuth 1.0a User Context)
TWITTER_API_KEY=...
TWITTER_API_SECRET=...
TWITTER_ACCESS_TOKEN=...
TWITTER_ACCESS_SECRET=...

# Google Cloud Vertex AI (primary LLM + Imagen)
# Paste entire service account JSON, or base64-encode it for Railway
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"gen-lang-client-0281408352",...}
# Legacy: also supports VERTEX_SA_KEY (same format)

# Gemini API (fallback for villain NFT generation if Vertex unavailable)
GEMINI_API_KEY=...

# NFT.Storage API (for IPFS uploads)
NFT_STORAGE_API_KEY=...

# -------------------------------------------------------------------
# Supabase SQL — Run in Supabase SQL Editor to create tables:
# -------------------------------------------------------------------
#
# CREATE TABLE chum_state (
#   id integer PRIMARY KEY DEFAULT 1 CHECK (id = 1),
#   balance numeric NOT NULL DEFAULT 0,
#   burn_rate numeric NOT NULL DEFAULT 0.5,
#   health_percent numeric NOT NULL DEFAULT 100,
#   mood text NOT NULL DEFAULT 'content',
#   brain_tier integer NOT NULL DEFAULT 0,
#   total_revenue numeric NOT NULL DEFAULT 0,
#   revenue_today numeric NOT NULL DEFAULT 0,
#   updated_at timestamptz NOT NULL DEFAULT now()
# );
# INSERT INTO chum_state (id) VALUES (1);
#
# CREATE TABLE thoughts (
#   id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
#   content text NOT NULL,
#   mood text NOT NULL DEFAULT 'content',
#   tweeted boolean NOT NULL DEFAULT false,
#   created_at timestamptz NOT NULL DEFAULT now()
# );
#
# CREATE TABLE transactions (
#   id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
#   type text NOT NULL CHECK (type IN ('revenue', 'donation', 'expense', 'burn')),
#   amount numeric NOT NULL,
#   description text,
#   signature text,
#   created_at timestamptz NOT NULL DEFAULT now()
# );
#
# CREATE TABLE villains (
#   id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
#   wallet_address text NOT NULL UNIQUE,
#   image_url text NOT NULL,
#   metadata_url text NOT NULL,
#   traits jsonb NOT NULL,
#   donation_amount numeric NOT NULL,
#   mint_signature text,
#   created_at timestamptz NOT NULL DEFAULT now()
# );
#
# CREATE INDEX idx_villains_wallet ON villains(wallet_address);
# CREATE INDEX idx_villains_created_at ON villains(created_at DESC);
